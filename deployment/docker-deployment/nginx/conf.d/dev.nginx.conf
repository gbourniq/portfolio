#
# Redirect all non-encrypted to encrypted
#
server {
    server_name          dev.gbournique.com;
    listen               *:80;
    listen               [::]:80;

    return 301 https://dev.gbournique.com$request_uri;
}

#
# There we go!
#
server {
    server_name          dev.gbournique.com;
    ssl_certificate      /etc/nginx/certs/cert_chain.crt;
    ssl_certificate_key  /etc/nginx/certs/www_gbournique_com.key;
    listen               *:443 ssl;
    listen               [::]:443 ssl;

    location / {
        proxy_pass http://app:8080;
        # proxy_set_header Host $host;
        # proxy_set_header X-Real-IP $remote_addr;
        # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /staticfiles {
        autoindex on;
        alias /www/staticfiles/;
    }

    location /mediafiles {
        autoindex on;
        alias /www/mediafiles/;
    }
}
