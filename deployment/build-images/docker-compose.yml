version: "3.7"

services:

  nginx:
    build:
      context: ../..
      dockerfile: deployment/build-images/nginx.dockerfile
    container_name: nginx
    restart: always
    volumes: 
      - static:/www/staticfiles/
      - media:/www/mediafiles/
    healthcheck:
      test: stat /etc/nginx/conf.d/django.conf && curl localhost:80 || exit 1
      interval: 120s
      timeout: 5s
      retries: 3
      start_period: 20s
    ports: 
      - "${PROXY_PORT}:${PROXY_PORT_INTERNAL}"
    networks:
      - proxy
    depends_on:
      - app
      
  app:
    build:
      context: ../..
      dockerfile: deployment/build-images/app.dockerfile
      args:
        app_component : "backend-app"
        app_debug: ${DEBUG}
    container_name: app
    restart: "no"
    env_file:
      - ./config/app.env
    environment:
      Debug: ${DEBUG}
    volumes:
      - static:/code/staticfiles/
      - media:/code/mediafiles/
      - redisdata:/data
    healthcheck:
      test: curl -f -s -S http://localhost:${APP_PORT} || exit 1
      interval: 10s
      timeout: 5s
      retries: 3
    expose: 
      - ${APP_PORT}
    networks:
      - backend
      - proxy
    entrypoint:
      - ./run_server.sh
    depends_on:
      - postgres
      - redis

  postgres:
    build:
      context: ../..
      dockerfile: deployment/build-images/postgres.dockerfile
    container_name: postgres
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data/ 
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 3
    expose:
      - 5432
    networks:
      - backend

  redis:
    build:
      context: ../..
      dockerfile: deployment/build-images/redis.dockerfile
    container_name: redis
    restart: "no"
    volumes:
      - redisdata:/data
    healthcheck:
      test: bash /usr/local/bin/run_redis_healthcheck.sh
      interval: 10s
      timeout: 5s
      retries: 3
    expose:
      - 6379
    networks:
      - backend
  
  celery:
    build:
      context: ../..
      dockerfile: deployment/build-images/app.dockerfile
      args:
        app_component : "backend-celery"
        app_debug: ${DEBUG}
    container_name: celery
    restart: "no"
    env_file:
      - ./config/app.env
    environment:
      Debug: ${DEBUG}
    volumes:
      - static:/code/main/static/
      - media:/code/main/mediafiles/
      - redisdata:/data
    healthcheck:
      test: ps | grep celery || exit 1
      interval: 10s
      timeout: 5s
      retries: 3
    expose: 
      - ${APP_PORT}
    networks:
      - backend
      - proxy
    entrypoint:
      - ./run_celery.sh
    depends_on:
      - postgres
      - redis

volumes:
  static:
  media:
  pgdata:
  redisdata:

networks:
  backend:
    driver: bridge
  proxy:
    driver: bridge