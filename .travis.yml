---
sudo: required

services:
  - docker

env:
  DOCKER_COMPOSE_VERSION: 1.23.2

before_install:
  # Set required environment variables and awscli configuration
  - source .env
  - ./utils/env_vars_check.sh

install:
  # Install docker-compose
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  # Install conda
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
  - bash miniconda.sh -b -p $HOME/miniconda
  - source "$HOME/miniconda/etc/profile.d/conda.sh"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda info -a
  # sshpass for Ansible to use ssh password authentication
  # macOS: brew install https://raw.githubusercontent.com/kadwanev/bigboybrew/master/Library/Formula/sshpass.rb
  - sudo apt-get install sshpass
  # For long running command without any output for < 60 minutes
  - travis_wait 60 mvn install

script:
  - make env
  - conda activate portfolio
  - make unit-tests
  - make portfolio
  - make latest
  - make up
  - make down
  - make docker-login
  - make publish-latest
  - make docker-deployment-playbook
